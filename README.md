# simple_clean_combine
A simple pipeline for cleaning and combining participant data. All data is artificial. To run, simply pass the path for the 'contact' file, the 'other' 
file, and the desired output path. 
